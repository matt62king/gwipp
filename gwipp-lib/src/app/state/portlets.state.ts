export interface PortletsState {
  blocks?: boolean;
  buttons?: boolean;
  grids?: boolean;
  inputs?: boolean;
  interactive?: boolean;
  lists?: boolean;
  menus?: boolean;
  overlays?: boolean;
}
